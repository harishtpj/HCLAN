# Program to Input value from user and output basic arith operations

let a := num(input("Enter no 1: "));
let b := num(input("Enter no 2: "));

println "a + b = " + (a + b);
println "a - b = " + (a - b);
println "a * b = " + (a * b);
println "a / b = " + (a / b);